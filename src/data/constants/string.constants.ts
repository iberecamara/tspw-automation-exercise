/** Common string literals reused across the framework, kept as named constants to avoid scattering raw `"\n"`/`""`/`" "` literals throughout the codebase. */
export const NEWLINE = "\n";
export const EMPTY = "";
export const SPACE = " ";
